import { StructuralTestDirective } from './structural-test.directive';

describe('StructuralTestDirective', () => {
  it('should create an instance', () => {
    const directive = new StructuralTestDirective();
    expect(directive).toBeTruthy();
  });
});
